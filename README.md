Nativetalk - A website for registering new users on a VOIP platform.

This application was built using React, with Bootstrap for styling purposes. The form library used is the react-hook-form library.

To run this app locally, clone the repository and run `npm install`
